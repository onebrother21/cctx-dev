import { Component } from "@angular/core";
import { Observable } from "rxjs";
//import { AppService,copy$ } from "@state";

@Component({selector:"oba-loader",templateUrl: "./loader.html",})

export class LoaderComponent {
  //copy:Observable<string>;
  //constructor(private app:AppService){this.copy = this.app.load(copy$);}
}
